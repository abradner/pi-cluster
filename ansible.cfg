[defaults]
host_key_checking = False
inventory = hosts.ini

[ssh_connection]
# Force macOS to send the Pi's native locale, suppressing the warning that breaks sudo
ssh_args = -C -o ControlMaster=auto -o ControlPersist=60s -o SetEnv="LC_ALL=C.UTF-8 LANG=C.UTF-8"
